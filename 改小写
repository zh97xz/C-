#include<stdio.h>
#include<ctype.h>

int main()
{
	char s[] = "I AM A STUDENT";
	int i = 1;
	while (s[i])
	{
		if (isupper(s[i]))
		{  
		s[i] = tolower(s[i]);
        }
		i++;
	}
	printf("%s\n", s);
	return 0;
}
