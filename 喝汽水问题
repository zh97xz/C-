#include<stdio.h>

int main()
{
	int money = 0;
	scanf_s("%d", &money);
	int total = 0;
	total = money;
	int empty = money;
	while (empty >= 2)
	{
		total += empty/ 2;
		empty = (empty/ 2 + empty % 2);
	}

	printf("total=%d", total);
return 0;
}
